# 80删除有序数组中的重复项

相关标签：[数组](https://leetcode.cn/tag/array/)   [双指针](https://leetcode.cn/tag/two-pointers/)

## 题目

给你一个有序数组 `nums` ，请你**[ 原地](http://baike.baidu.com/item/原地算法)** 删除重复出现的元素，使得出现次数超过两次的元素**只出现两次** ，返回删除后数组的新长度。

不要使用额外的数组空间，你必须在 **[原地 ](https://baike.baidu.com/item/原地算法)修改输入数组** 并在使用 O(1) 额外空间的条件下完成

**示例 1：**

```
输入：nums = [1,1,1,2,2,3]
输出：5, nums = [1,1,2,2,3]
解释：函数应返回新长度 length = 5, 并且原数组的前五个元素被修改为 1, 1, 2, 2, 3。 不需要考虑数组中超出新长度后面的元素。
```

**示例 2：**

```
输入：nums = [0,0,1,1,1,1,2,3,3]
输出：7, nums = [0,0,1,1,2,3,3]
解释：函数应返回新长度 length = 7, 并且原数组的前七个元素被修改为 0, 0, 1, 1, 2, 3, 3。不需要考虑数组中超出新长度后面的元素。
```

**提示：**

- `1 <= nums.length <= 3 * 104`
- `-104 <= nums[i] <= 104`
- `nums` 已按升序排列

## 解题思路

> 其实我们可以通过工具函数很快的计算出结果，但是本题考研的是双指针（快慢指针），所以还是要看一下思路
>
> 首先通过题目描述以及提示，我们得知`nums`已经是升序排列了，而我们要统计的是重复出现`2次`的元素，所以得知我们的循环下标可以从`2`开始。

```js
// nums = [0,0,1,1,1,1,2,3,3]
/**
 * @param {number[]} nums
 * @return {number}
 */
var removeDuplicates = function(nums) {
    // 先记录一下开始的位置，这里不是下标
    let i = 2;
    // 首先判断下入参nums的长度, 如果小于等于2则直接返回，肯定是满足条件的
    if (nums.length <= 2) {
        return nums.length;
    }
    // 再来看下入参nums长度大于2的情况
    for (let j = 2; j < = nums.length; j++) {
        if (nums[j] !== nums[i-2]} {
            nums[i] = nums[j];
            i++;
        }
    }
}
```

![](F:\JavaScript-Charles\LeetCode\images\微信图片_20260131001747_9_4.png)
